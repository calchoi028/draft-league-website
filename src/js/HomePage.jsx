import React from "react";

/**
 * HomePage
 * @author  
 */

const HomePage = () => {
    // TODO: Design Home page in a way that uses React Components to break things down
    // Suggestion: Break the page down into sections. Sections that are similar could
    // make use of a re-usable component in separate file.

    /*
     * -----------------------------------------------------
     * Render Helpers
     * -----------------------------------------------------
     */

    /*
     * -----------------------------------------------------
     * Main Render
     * -----------------------------------------------------
     */

    function render() {
        return (
            <div>Hello World!</div>
        )
    }

    return render();
}

export default HomePage;